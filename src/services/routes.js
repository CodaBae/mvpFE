const baseUrl = 'http://178.63.13.157:8090/mock-api/api';


const urls = {
    user: `${baseUrl}/users`,
    projects: `${baseUrl}/projects`,
    gateways: `${baseUrl}/gateways`,
    reports: `${baseUrl}/report`
}

export default urls